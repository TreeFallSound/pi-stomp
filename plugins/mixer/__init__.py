"""Mixer panel package marker.

Registration lives in ``plugins.pinned_params`` alongside the existing
MOD_MIXER_URI / MOD_MIXER_STEREO_URI customization (display_name, pinned_params,
panel_cls). This package only contains the panel implementation.
"""
